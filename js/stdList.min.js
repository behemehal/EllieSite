var errors = [{
        "code": "0x00",
        "title": "ClassNameRule",
        "detail": "Class names should follow HighCamelCase naming conventions. 'thisIs4Class' is wrong 'ThisIsClass' is correct",
    },
    {
        "code": "0x01",
        "title": "VariableNameRule",
        "detail": "Variable names should follow lowCamelCase naming conventions. 'ThisIsParam' is wrong 'thisIsParam' is correct",
    },
    {
        "code": "0x02",
        "title": "FunctionParameterRule",
        "detail": "Function parameter names should follow lowCamelCase naming conventions. 'ThisIsFunctionParam' is wrong 'thisIsFunctionParam' is correct",
    }
];
let light = !1;
for (var error, i = 0; i < errors.length; i++)
    if (error = errors[i], !error.ignored) {
        var a = document.createElement("a");
        a.innerHTML = error.code, a.href = `#${error.code}`, document.getElementById("btns").appendChild(a);
        var div = document.createElement("div");
        div.id = error.code, div.className = light ? "dark-section section" : "light-section section";
        var h3 = document.createElement("h3");
        h3.innerHTML = `#${error.code} - ${error.title}`;
        var h5 = document.createElement("h5");
        h5.innerHTML = error.detail, div.appendChild(h3), div.appendChild(h5);
        var codeDiv = document.createElement("div");
        if (codeDiv.id = error.code + "e", div.appendChild(codeDiv), document.getElementById("errors").appendChild(div), error.example) {
            let a = new Codev(error.code + "e");
            a.supplyCode(error.example)
        }
        light = !light
    }